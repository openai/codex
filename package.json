{
  "description": "Tools for repo-wide maintenance.",
  "scripts": {
    "release": "cd codex-cli && npm run release",
    "format": "npx prettier --check \"**/*.{json,md,yml,tsx}\"",
    "format:fix": "npx prettier --write \"**/*.{json,md,yml,tsx}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
