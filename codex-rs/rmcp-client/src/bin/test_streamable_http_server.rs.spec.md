## Overview
`test_streamable_http_server` runs the shared MCP test server behind the streamable HTTP transport. It reuses the same tool/resource surface described in `mod.spec.md`, optionally requiring a bearer token via the `MCP_EXPECT_BEARER` environment variable.
