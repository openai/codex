## Overview
`test_stdio_server` launches the stdio transport variant of the MCP test server with resource listings enabled, as noted in the shared binary summary (`mod.spec.md`). It exposes the echo tool alongside memo resources/templates for integration tests.
