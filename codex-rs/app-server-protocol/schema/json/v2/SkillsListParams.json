{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SkillRoot": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  "properties": {
    "additionalRoots": {
      "description": "Additional skill roots to scan alongside the defaults.",
      "items": {
        "$ref": "#/definitions/SkillRoot"
      },
      "type": "array"
    },
    "cwds": {
      "description": "When empty, defaults to the current session working directory.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "forceReload": {
      "description": "When true, bypass the skills cache and re-scan skills from disk.",
      "type": "boolean"
    }
  },
  "title": "SkillsListParams",
  "type": "object"
}