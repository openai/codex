load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "core-skills",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ),
    crate_name = "codex_core_skills",
    test_sizes = {
        "core-skills-unit-tests-windows-cross": "medium",
    },
)
