common --remote_download_minimal
common --nobuild_runfile_links
common --keep_going

# Remote execution currently uses a minimal Linux container that lacks audio system
# libraries and libclang. Run builds locally for now so voice dependencies resolve.
common:macos --strategy=TestRunner=darwin-sandbox,local

common:windows --strategy=TestRunner=local
