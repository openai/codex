name: r2-smoke

on:
  push:
    branches:
      - zsol/r2-smoke-20260714

permissions: {}

jobs:
  smoke:
    permissions:
      contents: read
    uses: ./.github/workflows/r2-release.yml
    secrets: inherit
    with:
      tag: rust-v0.145.0-alpha.9
