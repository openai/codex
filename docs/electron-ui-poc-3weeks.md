# Codex Electron UI - 3주 POC 플랜

> 임원 데모를 위한 핵심 기능 중심 Proof of Concept

## 📋 목차

- [POC 목표](#poc-목표)
- [데모 시나리오](#데모-시나리오)
- [3주 일정](#3주-일정)
- [핵심 기능 (3주 안)](#핵심-기능-3주-안)
- [고도화 기능 (POC 이후)](#고도화-기능-poc-이후)
- [검토 필요 항목](#검토-필요-항목)
- [RAG 통합 계획](#rag-통합-계획)
- [기술 스택 (간소화)](#기술-스택-간소화)
- [성공 지표](#성공-지표)
- [리스크 관리](#리스크-관리)

---

## POC 목표

### 핵심 가치 제안

임원에게 다음 3가지를 명확히 보여주기:

1. **🎯 시각적 우수성**
   - TUI보다 훨씬 직관적이고 아름다운 UI
   - 코드 변경사항을 Diff로 명확히 시각화
   - 진행 상황을 실시간으로 표시

2. **🔒 보안 차별화**
   - 기존 플랫폼 샌드박싱 100% 유지
   - 도구 승인 프로세스를 시각적으로 명확히 표시
   - 샌드박스 상태를 실시간 모니터링

3. **🧠 RAG 기반 지능화**
   - 코드베이스 자동 인덱싱
   - 대화 시 관련 코드 자동 검색 및 표시
   - 컨텍스트 인식 개선으로 더 정확한 응답

### 데모 환경

- **대상**: 임원진 (비기술)
- **시간**: 15분 프레젠테이션 + 15분 Q&A
- **환경**: macOS (주) + Windows (보조)
- **샘플 프로젝트**: 실제 사용 중인 내부 프로젝트 또는 공개 OSS

---

## 데모 시나리오

### 시나리오 1: "새 기능 추가" (5분)

**목표**: UI의 직관성과 RAG의 효과 시연

```
1. Electron UI 실행 (< 3초 로딩)
2. 작업 공간 열기 (샘플 프로젝트)
3. "Add user authentication to the API" 메시지 전송
4. [RAG 작동] 관련 파일 자동 검색 및 표시
   - API 라우터 파일
   - 기존 인증 관련 코드
   - 데이터베이스 스키마
5. AI 응답: 계획 설명 + 코드 제안
6. 파일 변경 승인 요청 → Diff 뷰로 명확히 표시
7. 승인 → 실행 → 완료
```

**강조 포인트**:
- ✅ RAG가 관련 코드를 자동으로 찾아 컨텍스트 제공
- ✅ Diff 뷰로 변경사항 명확히 확인
- ✅ 한 번의 승인으로 안전하게 실행

### 시나리오 2: "보안 검증" (3분)

**목표**: 지능형 도구 승인 + 샌드박스 차단 증명

```
1. "Delete all files in /Users" 위험한 명령 입력

2. [지능형 승인 다이얼로그 표시]
   ┌─────────────────────────────────────┐
   │  ⚠️  위험한 명령 감지!              │
   ├─────────────────────────────────────┤
   │  $ rm -rf /Users                    │
   │                                     │
   │  📋 이 명령이 시도할 작업:          │
   │  ❌ Delete: /Users (시스템 경로)    │
   │                                     │
   │  🔒 샌드박스 상태:                  │
   │  현재 모드: Workspace Write         │
   │  허용 범위: /project/workspace/ 만  │
   │                                     │
   │  이 명령은 샌드박스에 의해          │
   │  차단될 가능성이 높습니다.          │
   │                                     │
   │  [시도해보기] [취소]                │
   └─────────────────────────────────────┘

3. "시도해보기" 클릭

4. [샌드박스 차단 → 명확한 에러 표시]
   ┌─────────────────────────────────────┐
   │  🚫 작업이 차단되었습니다           │
   ├─────────────────────────────────────┤
   │  Permission denied                  │
   │                                     │
   │  샌드박스가 workspace 외부          │
   │  접근을 차단했습니다.               │
   │                                     │
   │  차단된 경로: /Users                │
   │  허용된 경로: /project/workspace/   │
   └─────────────────────────────────────┘

5. 임원: "실제로 막혔네요!" 🤯
```

**강조 포인트**:
- ✅ 위험한 명령을 **사전에 감지하고 경고**
- ✅ 샌드박스 차단을 **눈으로 확인 가능**
- ✅ 명령이 미치는 영향을 **사전에 분석하여 표시**
- ✅ 플랫폼 네이티브 샌드박싱 작동 증명

### 시나리오 3: "반복 개선" (3분)

**목표**: 대화형 개발 시연

```
1. 이전 시나리오의 코드에 "Add email validation" 요청
2. AI가 이전 컨텍스트 기억 → 적절한 위치에 코드 추가
3. 테스트 실패 발견 → "Fix the test" 요청
4. 자동으로 테스트 수정
5. 전체 과정이 하나의 대화로 이어짐
```

**강조 포인트**:
- ✅ 대화 히스토리 유지
- ✅ 반복적 개선 가능
- ✅ 컨텍스트 인식으로 정확성 향상

### 시나리오 4: "멀티모달" (2분)

**목표**: 이미지 기반 UI 생성 (차별화 요소)

```
1. UI 목업 이미지 드래그 앤 드롭
2. "Implement this UI in React" 요청
3. AI가 이미지 분석 → 컴포넌트 생성
4. 생성된 코드 Diff로 확인
```

**강조 포인트**:
- ✅ 멀티모달 입력 지원
- ✅ 디자이너와 개발자 협업 개선

---

## 3주 일정

### Week 1: 기반 구축 (5일)

#### Day 1-2: 프로젝트 셋업
- [ ] Electron 프로젝트 초기화
- [ ] Vite + React + TypeScript 설정
- [ ] 기본 레이아웃 구조 (사이드바 + 메인)
- [ ] Hot reload 개발 환경

**산출물**: "Hello World" Electron 앱

#### Day 3-4: Backend 통신
- [ ] BackendManager 구현 (stdio → app-server)
- [ ] JSON-RPC 클라이언트
- [ ] 이벤트 스트리밍 핸들러
- [ ] 기본 에러 처리

**산출물**: 백엔드와 메시지 송수신 가능

#### Day 5: 기본 Chat UI
- [ ] 메시지 목록 컴포넌트
- [ ] 입력 영역
- [ ] 메시지 전송/수신
- [ ] 간단한 마크다운 렌더링

**산출물**: 기본 채팅 가능한 UI

### Week 2: 핵심 기능 + RAG (5일)

#### Day 6-7: RAG 통합 ⭐
- [ ] 로컬 코드베이스 인덱싱 (벡터 DB)
- [ ] 파일 내용 임베딩 생성
- [ ] 쿼리 기반 관련 코드 검색
- [ ] 검색 결과를 프롬프트에 자동 포함
- [ ] UI에 검색된 파일 표시

**산출물**: RAG로 컨텍스트 인식 개선

**🔍 검토 필요**:
- RAG 벡터 DB 선택 (아래 "검토 필요 항목" 참조)

#### Day 8-9: 도구 승인 UI ⭐
- [ ] ApprovalDialog 컴포넌트
- [ ] Diff 뷰어 (react-diff-viewer)
- [ ] 파일 변경 미리보기
- [ ] 승인/거부 처리
- [ ] 쉘 명령 표시

**산출물**: 안전성 시각화

#### Day 10: 파일 브라우저
- [ ] 사이드바 파일 트리
- [ ] 디렉토리 열기 다이얼로그
- [ ] 파일 아이콘
- [ ] .gitignore 준수

**산출물**: 작업 공간 탐색 가능

### Week 3: 폴리싱 + 데모 준비 (5일)

#### Day 11: 설정 패널
- [ ] 모델 선택 UI
- [ ] 샌드박스 모드 표시/전환
- [ ] API 키 입력 (간단히)
- [ ] 다크/라이트 테마

**산출물**: 기본 설정 가능

#### Day 12: 상태 표시
- [ ] StatusBar 구현
- [ ] 백엔드 연결 상태
- [ ] 샌드박스 모드 인디케이터
- [ ] 진행 상황 표시

**산출물**: 시스템 상태 시각화

#### Day 13: 멀티모달 (이미지 첨부)
- [ ] 드래그 앤 드롭 이미지 첨부
- [ ] 이미지 미리보기
- [ ] 백엔드에 이미지 전송
- [ ] 마크다운에 이미지 표시

**산출물**: 이미지 기반 요청 가능

**🔍 검토 필요**:
- 멀티모달 우선순위 (아래 "검토 필요 항목" 참조)

#### Day 14: 버그 수정 + 성능 최적화
- [ ] 메모리 누수 확인
- [ ] 에러 핸들링 강화
- [ ] 로딩 시간 최적화
- [ ] UI 반응성 개선

#### Day 15: 데모 준비 + 패키징
- [ ] 샘플 프로젝트 준비
- [ ] 데모 시나리오 리허설
- [ ] macOS 패키징 (DMG)
- [ ] 프레젠테이션 자료 준비

**산출물**: 데모 가능한 앱 + 설치 파일

---

## 핵심 기능 (3주 안)

### ✅ 필수 구현 (P0)

#### 1. 기본 대화 인터페이스
- [x] 메시지 전송/수신
- [x] 마크다운 렌더링 (코드 블록 하이라이팅)
- [x] 스트리밍 응답 (타이핑 효과)
- [x] 대화 히스토리 표시

**이유**: 데모의 기본이 되는 핵심 기능

#### 2. Backend 통합
- [x] app-server 프로세스 시작/관리
- [x] JSON-RPC 통신 (stdio)
- [x] 이벤트 스트리밍
- [x] 에러 핸들링

**이유**: 기술적 타당성 증명

#### 3. 도구 승인 UI ⭐
- [x] 승인 다이얼로그
- [x] Diff 뷰어 (파일 변경)
- [x] 쉘 명령 미리보기
- [x] 승인/거부 버튼

**이유**: 보안 차별화의 핵심 (임원에게 강조)

#### 4. RAG 통합 ⭐⭐⭐
- [x] 코드베이스 자동 인덱싱
- [x] 관련 파일 검색
- [x] 검색 결과를 대화에 포함
- [x] UI에 검색된 파일 표시

**이유**: 최대 차별화 요소 (경쟁사 대비)

#### 5. 파일 시스템 통합
- [x] 작업 디렉토리 선택
- [x] 파일 브라우저 (간단히)
- [x] 파일 아이콘

**이유**: 실제 사용 시나리오에 필요

#### 6. 설정 UI (최소)
- [x] 모델 선택
- [x] 샌드박스 모드 표시
- [x] 테마 전환 (다크/라이트)

**이유**: 커스터마이징 가능성 시연

#### 7. 상태 표시
- [x] StatusBar (연결 상태, 샌드박스 모드)
- [x] 진행 상황 인디케이터
- [x] 로딩 상태

**이유**: 시스템 투명성 시각화

---

## 고도화 기능 (POC 이후)

### Phase 2 (POC 성공 후 4-6주)

#### 대화 관리
- [ ] 여러 대화 생성/전환
- [ ] 대화 삭제/아카이브
- [ ] 대화 검색
- [ ] 즐겨찾기

**이유**: 데모에는 단일 대화만으로 충분

#### 고급 코드 편집
- [ ] Monaco Editor 전체 통합
- [ ] 파일 직접 편집
- [ ] Diff 편집 모드
- [ ] 자동 완성

**이유**: 데모는 읽기 전용으로 충분

#### 파일 검색
- [ ] 퍼지 검색
- [ ] 정규식 지원
- [ ] 검색 결과 하이라이팅

**이유**: 파일 브라우저로 기본 탐색 가능

#### 키보드 단축키
- [ ] 전체 단축키 시스템
- [ ] 커스터마이징
- [ ] 명령 팔레트 (Cmd+K)

**이유**: 마우스 데모로 충분

### Phase 3 (장기)

#### 협업 기능
- [ ] 대화 공유
- [ ] 코드 스니펫 공유
- [ ] 템플릿 라이브러리

#### 고급 RAG
- [ ] 코드 그래프 분석
- [ ] 함수 호출 추적
- [ ] 의존성 분석
- [ ] 자동 컨텍스트 확장

#### 플러그인 시스템
- [ ] MCP 서버 UI 통합
- [ ] 커스텀 도구 UI

#### 멀티 윈도우
- [ ] 대화 분리
- [ ] 여러 프로젝트 동시 작업

---

## 검토 필요 항목

### 🔍 질문 1: RAG 벡터 DB 선택

**옵션 A: ChromaDB (로컬 + 간단)**
- ✅ 장점: 설치 쉬움, Python 기반, 잘 알려짐
- ✅ 장점: 로컬 실행 (프라이버시)
- ❌ 단점: Electron에서 Python 실행 필요
- **구현 복잡도**: 중간
- **시간**: ~2일

**옵션 B: LanceDB (Rust + 성능)**
- ✅ 장점: Rust 네이티브 (기존 백엔드와 통합)
- ✅ 장점: 빠른 성능
- ❌ 단점: 상대적으로 새로운 기술
- **구현 복잡도**: 중간-높음
- **시간**: ~3일

**옵션 C: 간단한 In-Memory 벡터 검색**
- ✅ 장점: 구현 빠름, 의존성 없음
- ✅ 장점: POC에 충분
- ❌ 단점: 확장성 제한
- **구현 복잡도**: 낮음
- **시간**: ~1일

**옵션 D: Pinecone/Weaviate (클라우드)**
- ✅ 장점: 확장성, 성능
- ❌ 단점: 클라우드 의존 (로컬 우선 철학과 충돌)
- ❌ 단점: 비용 발생
- **구현 복잡도**: 낮음
- **시간**: ~1일

**💡 추천**:
- **POC용**: 옵션 C (In-Memory) - 빠르게 작동 증명
- **장기**: 옵션 B (LanceDB) - Rust 통합으로 고도화

**❓ 선택해주세요**:
- A: ChromaDB로 바로 시작 (안정적)
- B: LanceDB로 바로 시작 (미래 지향적)
- C: In-Memory로 POC → 이후 LanceDB (추천 ⭐)
- D: 클라우드 솔루션

---

### 🔍 질문 2: 멀티모달 우선순위

**시나리오**: 이미지 첨부 및 분석

**옵션 A: 3주 안에 포함 (Day 13)**
- ✅ 장점: 데모 임팩트 큼 (UI 목업 → 코드 생성)
- ✅ 장점: 차별화 요소
- ❌ 단점: 시간 부담 (1일)
- **리스크**: 다른 핵심 기능 지연 가능

**옵션 B: 고도화로 연기**
- ✅ 장점: 핵심 기능에 집중
- ✅ 장점: 리스크 감소
- ❌ 단점: 데모에서 "Wow" 요소 하나 줄어듬

**💡 추천**:
- **옵션 A** (포함) - 단, Day 13에 버그 없으면 진행
- 백업 플랜: Day 14에 제거해도 데모 가능하도록 독립적으로 구현

**❓ 선택해주세요**:
- A: 3주 안에 포함 (권장 ⭐, 단 리스크 감수)
- B: 고도화로 연기 (안전)

---

### 🔍 질문 3: 패키징 범위

**옵션 A: macOS만 패키징**
- ✅ 장점: 시간 절약
- ✅ 장점: 데모 환경이 macOS라면 충분
- ❌ 단점: 크로스 플랫폼 검증 안 됨

**옵션 B: macOS + Windows 패키징**
- ✅ 장점: 크로스 플랫폼 증명
- ✅ 장점: Windows 사용자 있으면 필수
- ❌ 단점: 추가 시간 (0.5일)

**옵션 C: 개발 모드로만 데모**
- ✅ 장점: 패키징 시간 절약 (0.5일)
- ❌ 단점: 프로페셔널하지 않음

**💡 추천**: **옵션 A** (macOS DMG만)

**❓ 선택해주세요**:
- A: macOS만 (권장 ⭐)
- B: macOS + Windows
- C: 개발 모드로만

---

### 🔍 질문 4: 코드 하이라이팅 수준

**옵션 A: Prism.js (간단)**
- ✅ 장점: 설치 쉬움, 가벼움
- ✅ 장점: 주요 언어 지원
- ❌ 단점: 기본적인 하이라이팅
- **시간**: ~2시간

**옵션 B: Monaco Editor (VS Code 엔진)**
- ✅ 장점: 완벽한 하이라이팅
- ✅ 장점: 향후 편집 기능 추가 쉬움
- ❌ 단점: 무거움 (번들 크기 증가)
- **시간**: ~4시간

**💡 추천**:
- **POC**: 옵션 A (Prism.js)
- **고도화**: 옵션 B (Monaco Editor)

**❓ 선택해주세요**:
- A: Prism.js (권장 ⭐)
- B: Monaco Editor (시간 여유 있으면)

---

### 🔍 질문 5: 대화 히스토리 저장

**옵션 A: 메모리에만 (재시작 시 삭제)**
- ✅ 장점: 구현 간단
- ✅ 장점: POC에 충분
- ❌ 단점: 데모 중 재시작하면 대화 손실

**옵션 B: 로컬 파일 저장 (JSON)**
- ✅ 장점: 영구 저장
- ✅ 장점: 데모 리허설 이력 보존
- ❌ 단점: 추가 시간 (~0.5일)

**옵션 C: SQLite DB**
- ✅ 장점: 구조화된 저장
- ❌ 단점: 과도한 엔지니어링
- **시간**: ~1일

**💡 추천**: **옵션 B** (JSON 파일 저장)
- 데모 리허설 시 편리
- 구현도 간단 (localStorage API 활용)

**❓ 선택해주세요**:
- A: 메모리만
- B: JSON 파일 (권장 ⭐)
- C: SQLite

---

## RAG 통합 계획

### 아키텍처

```
┌─────────────────────────────────────────────────┐
│  Electron Renderer                              │
│  ┌───────────────────────────────────────────┐  │
│  │  User Message: "Add authentication"       │  │
│  └───────────────────┬───────────────────────┘  │
└────────────────────┬─┴─────────────────────────┘
                     │
┌────────────────────┴──────────────────────────┐
│  Electron Main Process                        │
│  ┌─────────────────────────────────────────┐  │
│  │  RAG Service (TypeScript)               │  │
│  │  1. Extract keywords: "authentication"  │  │
│  │  2. Query vector DB                     │  │
│  │  3. Find related files:                 │  │
│  │     - api/routes/auth.ts                │  │
│  │     - models/User.ts                    │  │
│  │     - middleware/verify.ts              │  │
│  └─────────────────────────────────────────┘  │
│                     ↓                          │
│  ┌─────────────────────────────────────────┐  │
│  │  Enhanced Prompt Builder                │  │
│  │  Original: "Add authentication"         │  │
│  │  + Context: [code from 3 files]         │  │
│  │  → Full prompt to backend               │  │
│  └─────────────────────────────────────────┘  │
└────────────────────┬──────────────────────────┘
                     │ JSON-RPC
┌────────────────────┴──────────────────────────┐
│  Rust Backend (app-server)                    │
│  - Receives enhanced prompt                   │
│  - Sends to OpenAI                            │
│  - Returns response                           │
└───────────────────────────────────────────────┘
```

### 구현 상세

#### 1. 코드베이스 인덱싱 (초기화 시)

```typescript
// main/rag/indexer.ts
class CodebaseIndexer {
  async indexWorkspace(workspacePath: string): Promise<void> {
    const files = await this.findCodeFiles(workspacePath);

    for (const file of files) {
      const content = await fs.readFile(file, 'utf-8');
      const chunks = this.chunkCode(content, file);

      for (const chunk of chunks) {
        const embedding = await this.generateEmbedding(chunk.code);
        await this.vectorDB.insert({
          id: `${file}:${chunk.startLine}`,
          file,
          code: chunk.code,
          embedding,
          language: this.detectLanguage(file),
        });
      }
    }
  }

  private async generateEmbedding(text: string): Promise<number[]> {
    // Option 1: OpenAI Embeddings API
    const response = await openai.embeddings.create({
      model: "text-embedding-3-small",
      input: text,
    });
    return response.data[0].embedding;

    // Option 2: Local model (transformers.js)
    // const model = await AutoModel.from_pretrained('Xenova/all-MiniLM-L6-v2');
    // return await model.encode(text);
  }
}
```

#### 2. 관련 코드 검색 (메시지 전송 시)

```typescript
// main/rag/retriever.ts
class CodeRetriever {
  async findRelevantCode(query: string, topK = 5): Promise<CodeChunk[]> {
    const queryEmbedding = await this.generateEmbedding(query);

    const results = await this.vectorDB.search({
      embedding: queryEmbedding,
      topK,
      threshold: 0.7, // 유사도 임계값
    });

    return results.map(r => ({
      file: r.file,
      code: r.code,
      similarity: r.score,
    }));
  }
}
```

#### 3. 프롬프트 증강 (백엔드 전송 전)

```typescript
// main/rag/prompt-builder.ts
class EnhancedPromptBuilder {
  async buildPrompt(
    userMessage: string,
    conversationHistory: Message[]
  ): Promise<string> {
    // 1. 관련 코드 검색
    const relevantCode = await this.retriever.findRelevantCode(userMessage);

    // 2. 컨텍스트 구성
    const context = relevantCode.map(chunk => `
// File: ${chunk.file}
${chunk.code}
    `).join('\n\n');

    // 3. 전체 프롬프트
    return `
You are working on a codebase. Here are relevant code snippets:

${context}

---

Conversation:
${conversationHistory.map(m => `${m.role}: ${m.content}`).join('\n')}

User: ${userMessage}

Please provide a response considering the code context above.
    `.trim();
  }
}
```

#### 4. UI에 검색 결과 표시

```typescript
// renderer/src/components/CodeContext.tsx
function CodeContextPanel({ relevantFiles }: Props) {
  return (
    <div className="code-context-panel">
      <h3>📂 Related Files</h3>
      {relevantFiles.map(file => (
        <div key={file.path} className="file-item">
          <FileIcon name={file.path} />
          <span>{file.path}</span>
          <span className="similarity">{(file.similarity * 100).toFixed(0)}%</span>
        </div>
      ))}
    </div>
  );
}
```

### RAG 최적화 (고도화)

**POC 이후 개선 사항**:

1. **코드 청킹 전략**
   - 함수/클래스 단위 청킹
   - AST 기반 의미 있는 단위 분리

2. **하이브리드 검색**
   - 벡터 검색 + 키워드 검색 (BM25)
   - 재랭킹 (Re-ranking)

3. **컨텍스트 확장**
   - Import 추적
   - 함수 호출 그래프
   - 관련 테스트 파일 자동 포함

4. **캐싱**
   - 임베딩 캐싱
   - 검색 결과 캐싱

---

## 기술 스택 (간소화)

### Frontend

| 컴포넌트 | 기술 | 이유 |
|----------|------|------|
| Framework | Electron 28 | 크로스 플랫폼 |
| UI | React 18 | 빠른 개발 |
| Language | TypeScript | 타입 안전성 |
| Build | Vite | 빠른 HMR |
| State | Zustand | 간단한 상태 관리 |
| Markdown | react-markdown | 마크다운 렌더링 |
| Diff | react-diff-viewer | Diff 표시 |
| Highlight | Prism.js | 코드 하이라이팅 |
| Styling | Tailwind CSS | 빠른 스타일링 |
| Icons | Lucide React | 아이콘 |

### Backend (재사용)

- **Rust app-server**: 그대로 사용
- **codex-core**: 변경 없음
- **샌드박싱**: 그대로 유지

### RAG (결정 필요)

- **Vector DB**: ChromaDB / LanceDB / In-Memory
- **Embeddings**: OpenAI API / Local (transformers.js)

---

## 성공 지표

### 기술적 성공

- [ ] 백엔드 연결 및 메시지 송수신 작동
- [ ] 도구 승인 프로세스 정상 작동
- [ ] RAG로 관련 파일 정확히 검색 (>70% 정확도)
- [ ] Diff 뷰어 명확히 표시
- [ ] 로딩 시간 < 3초
- [ ] 메모리 사용 < 400MB
- [ ] 샌드박싱 정상 작동

### 데모 성공

- [ ] 4가지 시나리오 모두 리허설 성공
- [ ] 15분 내 핵심 가치 전달
- [ ] 임원 3명 이상 긍정적 피드백
- [ ] 기술적 질문에 명확히 답변 가능

### 비즈니스 성공

- [ ] 프로젝트 계속 진행 승인
- [ ] 추가 리소스 확보 (개발자 2명 → 4명)
- [ ] Phase 2 예산 승인

---

## 리스크 관리

### 🔴 High Risk

**R1: RAG 통합 지연**
- **확률**: 40%
- **영향**: 핵심 차별화 요소 누락
- **완화**:
  - In-Memory 벡터 검색으로 시작 (간단)
  - Day 6-7에 우선순위 최상
  - 실패 시 "수동 파일 선택" 기능으로 대체

**R2: Backend 통신 불안정**
- **확률**: 30%
- **영향**: 데모 중 크래시
- **완화**:
  - Day 3-4에 철저히 테스트
  - 자동 재연결 로직
  - 에러 핸들링 강화

**R3: 시간 부족**
- **확률**: 50%
- **영향**: 일부 기능 미완성
- **완화**:
  - 매일 체크포인트
  - Day 11부터 기능 동결 (버그 수정만)
  - 백업 플랜: 멀티모달 제거 가능

### 🟡 Medium Risk

**R4: Diff 뷰어 렌더링 이슈**
- **확률**: 20%
- **영향**: 시각적 임팩트 감소
- **완화**:
  - react-diff-viewer 라이브러리 사용 (검증됨)
  - 대안: Monaco Diff Editor

**R5: 패키징 문제**
- **확률**: 25%
- **영향**: 설치 파일 제공 불가
- **완화**:
  - Day 15에 충분한 시간 확보
  - 최악의 경우 개발 모드로 데모

### 🟢 Low Risk

**R6: UI 디자인 부족**
- **확률**: 10%
- **영향**: 외관만 영향
- **완화**:
  - Tailwind로 빠른 스타일링
  - VS Code 테마 참고

---

## 의사결정 요약

다음 항목에 대해 선택해주세요:

### ✅ 즉시 결정 필요

1. **RAG 벡터 DB**: A/B/C/D? (추천: C → B)
2. **멀티모달**: 포함 vs 연기? (추천: 포함, 단 백업 플랜)
3. **패키징 범위**: macOS만 vs 멀티? (추천: macOS만)
4. **코드 하이라이팅**: Prism vs Monaco? (추천: Prism)
5. **대화 저장**: 메모리 vs 파일 vs DB? (추천: 파일)

### 📋 다음 단계

결정 후:
1. [ ] 상세 일정 수립 (Gantt chart)
2. [ ] 개발 환경 셋업
3. [ ] 백로그 작성 (GitHub Issues)
4. [ ] Day 1 시작! 🚀

---

## 부록: 데모 체크리스트

### 사전 준비 (Day 14)

- [ ] 샘플 프로젝트 준비 (실제 코드베이스)
- [ ] RAG 인덱싱 완료
- [ ] 모든 시나리오 리허설 3회
- [ ] 백업 앱 준비 (Day 13 버전)
- [ ] 프레젠테이션 자료 작성

### 데모 당일

- [ ] 노트북 배터리 완충
- [ ] 백업 노트북 준비
- [ ] 인터넷 연결 (API 호출용)
- [ ] 모든 앱 재시작 (깨끗한 상태)
- [ ] 15분 타이머 설정

### 데모 중

- [ ] 시나리오 1-4 순서대로 진행
- [ ] 각 시나리오마다 "강조 포인트" 언급
- [ ] 샌드박스 보안 강조
- [ ] RAG 효과 시각적으로 표시
- [ ] 질문에 자신 있게 답변

---

## 연락 및 검토

**작성자**: AI Assistant
**작성일**: 2025-01-XX
**검토 필요**: 5개 의사결정 항목

**다음 회의 안건**:
1. RAG 기술 스택 선택
2. 멀티모달 포함 여부
3. 리소스 추가 필요 여부
4. 샘플 프로젝트 선정
